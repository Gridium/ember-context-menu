<span class="context-menu__item__label">
  {{#if item.icon}}
    <i class="fa fa-{{item.icon}} context-menu__item__icon"></i>
  {{/if}}
  {{item.label}} {{#if _amount}}({{_amount}}){{/if}}
</span>

{{#if item.subActions}}
  {{#unless isDisabled}}
    <ul class="context-menu--sub">
      {{#each item.subActions as |subItem|}}
        {{context-menu-item
          item=subItem
          amount=amount
          itemIsDisabled=itemIsDisabled
          clickAction=clickAction
        }}
      {{/each}}
    </ul>
  {{/unless}}
{{/if}}
